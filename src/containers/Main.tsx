import { Categories } from './categories/Categories'
import { TodosWrapper } from './todos/TodosWrapper'

export const Main = () => {
    return (
        <main>
            <Categories />
            <TodosWrapper />
        </main>
    )
}
